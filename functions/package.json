{
  "name": "chess-selfcapture-functions",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "rm -f ./*.nnue && cp ../web/public/stockfish.js . && cp ../web/public/stockfish.wasm . && (cp ../web/public/stockfish.worker.js . 2>/dev/null || true)",
    "build": "tsc"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.9.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "private": true
}
